{
  "name": "off-piste-studio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync:insights": "node scripts/generate-insights.mjs",
    "dev": "npm run sync:insights && vite",
    "build": "npm run sync:insights && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
